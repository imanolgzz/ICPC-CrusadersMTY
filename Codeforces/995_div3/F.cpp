#include <bits/stdc++.h>

using namespace std;

void redirectIO(){
  freopen("input.in", "r", stdin);
  freopen("output.out", "w", stdout);
}

void solve(){

}

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  redirectIO();
  int t;
  cin >> t;
  while(t--){
    solve();
  }
  return 0;
}